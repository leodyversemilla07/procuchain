# Laravel application server is managed by Elastic Beanstalk (procuchain-prod environment).
# The EB-managed instance (i-03a0bba9a30023252) replaces the former Terraform-managed app server.
# MultiChain nodes and RDS remain Terraform-managed.
#
# App URL: http://procuchain-prod.eba-vujm352s.us-east-1.elasticbeanstalk.com
# EB environment: procuchain-prod (PHP 8.4 on AL2023, Nginx)
