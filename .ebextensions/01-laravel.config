# Minimal EB config — just document root and permissions
# No artisan commands, no DB connections, no hanging

option_settings:
  aws:elasticbeanstalk:container:php:phpini:
    document_root: /public
